<div class="mx-auto w-full max-w-md">
  <h2 class="mb-6 text-2xl font-bold">Edit event</h2>

  <%= render "form", event: @event %>

  <p class="mt-6 text-sm"><%= link_to "Back", event_path(@event), class: "text-blue-600 hover:underline" %></p>
</div>
